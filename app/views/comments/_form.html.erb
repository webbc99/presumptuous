<%= form_for([@discussion, @discussion.comments.build]) do |form| %>
  <%= form.text_area :content %>
  <div id="comment_submit_button">
    <button class="waves-effect waves-light btn-small" type="submit" name="action">Submit</button>
  </div>
  <br>
<% end %>